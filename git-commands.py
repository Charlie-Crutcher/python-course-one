# Commands for git.

clear_console = "clear" #duh
open_file_manager = "explorer"

check_version = "git --version"
check_directory = "pwd" #print working directory
check_contents = "ls"
check_contents_more_info = "ls -l"
check_contents_all_info = "ls -al"
check_repository_status = "git status" #Master means it has been initialized

change_directory = "cd 'FILEPATH'"
make_directory = "mkdir 'DIRECTORY NAME'"
initialize_directory = "git init"
create_file = "touch 'FILENAME'"
create_text_file = "echo 'words' > whichfile.txt"

add_a_repository_from_github = "git remote add origin 'SSH LINK'"
add_files = "git add 'filename'"
commit_files = "git commit -m 'comment goes here'"
select_branch = "git branch -m 'name of branch' (commonly main)"

push_files_to_github = "git push -u origin main"